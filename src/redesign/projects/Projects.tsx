import { useState } from 'react';
import { ArrowLeft, ArrowUpRight, CalendarDays, Check, FileText, FolderKanban, MessageCircle, Plus, Users } from 'lucide-react';
import { creators } from '../data'; import { useProductDomain } from '../domain/useProductDomain'; import type { Page } from '../types'; import './projects.css';
type Tab='Overview'|'Phases'|'Team'|'Files'|'Budget'|'Activity'; const tabs:Tab[]=['Overview','Phases','Team','Files','Budget','Activity'];
export default function Projects({notify,navigate}:{notify:(m:string)=>void;navigate:(p:Page)=>void}){const {state}=useProductDomain();const [selected,setSelected]=useState<string|null>(null);const [tab,setTab]=useState<Tab>('Overview');const [phase,setPhase]=useState('Pre-Production');const [approved,setApproved]=useState(false);const project=state.projects.find(p=>p.id===selected);
 if(project)return <main className="projects-page"><button className="projects-back" onClick={()=>setSelected(null)}><ArrowLeft/>All projects</button><header className="project-title"><div><p className="eyebrow">PROJECT WORKSPACE · LOCAL PREVIEW</p><h1>{project.title}</h1><p>{phase} · due {project.dueLabel}</p></div><div><button className="button secondary" onClick={()=>navigate('inbox')}><MessageCircle/>Conversation</button><button className="button primary" aria-label="Open linked tasks" onClick={()=>navigate('tasks')}>Open linked tasks<ArrowUpRight/></button></div></header><nav className="project-tabs" role="tablist">{tabs.map(x=><button role="tab" aria-selected={tab===x} key={x} onClick={()=>setTab(x)}>{x}</button>)}</nav><section className="project-panel">{tab==='Overview'&&<div className="project-overview"><article><p className="eyebrow">THE BRIEF</p><h2>A study of shape, light, and quiet.</h2><p>Create an editorial image story rooted in the Namib, built with credited collaborators and a restrained visual language.</p><div className="project-progress"><span style={{width:`${project.progress}%`}}/><p>{project.progress}% complete</p></div></article><aside><h3>Next milestone</h3><strong>Golden-hour production day</strong><p><CalendarDays/>18 Oct · Sossusvlei</p><button className="button secondary" onClick={()=>notify('Milestone reminder saved locally.')}>Set reminder</button></aside></div>}{tab==='Phases'&&<div className="phase-list">{['Development','Pre-Production','Production','Post-Production','Distribution'].map((x,i)=><button key={x} aria-pressed={phase===x} onClick={()=>{setPhase(x);notify(`Project moved to ${x} locally.`)}}><span>{i+1}</span><strong>{x}</strong>{phase===x&&<Check/>}</button>)}</div>}{tab==='Team'&&<div className="project-team">{creators.slice(0,4).map((p,i)=><article key={p.id}><img src={p.image} alt=""/><div><strong>{p.name}</strong><span>{i===0?'Creative lead':i===1?'Film lead':'Collaborator'}</span></div><progress max="100" value={75-i*12}/></article>)}<button className="button secondary" onClick={()=>navigate('talent')}><Plus/>Invite talent</button></div>}{tab==='Files'&&<div className="project-files"><article><FileText/><div><strong>Treatment_v3.pdf</strong><span>Version 3 · reviewed today</span></div><button className="button secondary" onClick={()=>setApproved(!approved)}>{approved?'Approved':'Request approval'}</button></article><article><FileText/><div><strong>Location_references.zip</strong><span>Version 2 · local metadata</span></div></article><p>Files are represented by local metadata until cloud storage is connected.</p></div>}{tab==='Budget'&&<div className="project-budget"><div><span>Planned</span><strong>N$ 42,000</strong></div><div><span>Recorded</span><strong>N$ 23,600</strong></div><div className="budget-remaining"><span>Available</span><strong>N$ 18,400 remaining</strong></div><button className="button secondary" onClick={()=>notify('Budget-line editor opened in this local preview.')}><Plus/>Add budget line</button></div>}{tab==='Activity'&&<ol className="project-activity"><li><strong>Phase changed</strong><span>Moved into {phase} · Today</span></li><li><strong>File reviewed</strong><span>Treatment_v3.pdf · Yesterday</span></li><li><strong>Leo joined the project</strong><span>Film lead · 2 days ago</span></li></ol>}</section></main>;
 return <main className="projects-page"><header className="projects-hero"><button className="icon-button" onClick={()=>navigate('tools')} aria-label="Back to creative tools"><ArrowLeft/></button><div><p className="eyebrow">THE WORK, IN MOTION</p><h1>Projects</h1><p>Shared bodies of work—from the first conversation to final delivery.</p></div><FolderKanban/></header><div className="projects-summary"><span><strong>{state.projects.length}</strong>active projects</span><span><strong>5</strong>collaborators</span><span><strong>2</strong>milestones this week</span></div><section className="project-cards">{state.projects.map((p,i)=><button key={p.id} aria-label={`Open ${p.title}`} onClick={()=>setSelected(p.id)}><span className={`project-card-art art-${i}`}><FolderKanban/></span><span className="project-card-copy"><small>{p.phase}</small><strong>{p.title}</strong><span><Users/> {i+2} collaborators</span><span className="project-card-progress"><i style={{width:`${p.progress}%`}}/></span><em>{p.progress}% · due {p.dueLabel}</em></span><ArrowUpRight/></button>)}</section></main>}
