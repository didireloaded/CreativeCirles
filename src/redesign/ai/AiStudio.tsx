import { useState } from 'react'; import { ArrowLeft, Bot, Check, Copy, FileText, Save, Sparkles } from 'lucide-react'; import { useProductDomain } from '../domain/useProductDomain'; import type { Page } from '../types'; import '../opportunities/opportunities.css'; import './ai.css';
const tools=['Shot List','Storyboard','Bio Optimizer','Pitch Builder','Pricing Calculator','Project Brief','Caption Helper','Contract'];
function generate(tool:string,subject:string){const name=subject.trim()||'your creative project';return tool==='Shot List'?`PREVIEW DRAFT\n1. Establishing wide — ${name}\n2. Intentional detail — hands and texture\n3. Human medium — quiet action\n4. Closing frame — room to breathe`:tool==='Contract'?`PREVIEW TEMPLATE\nParties: Creator and Client\nScope: ${name}\nDeliverables: To be agreed in writing\nUsage: Limited to the agreed campaign\nCancellation: Written notice required`:`PREVIEW DRAFT\n${name}\n\nA clear, human starting point shaped for ${tool.toLowerCase()}. Keep the language specific, show the creative intention, and name the next useful action.`}
export default function AiStudio({notify,navigate}:{notify:(m:string)=>void;navigate:(p:Page)=>void}){const {toggleSaved}=useProductDomain();const [tool,setTool]=useState('Shot List');const [subject,setSubject]=useState('Desert editorial at first light');const [output,setOutput]=useState('');const [history,setHistory]=useState<string[]>([]);return <main className="ai-page"><header className="ai-hero"><button className="icon-button" onClick={()=>navigate('tools')} aria-label="Back to creative tools"><ArrowLeft/></button><div><p className="eyebrow">LOCAL CREATIVE ASSISTANT</p><h1>AI Studio</h1><p>Structured preview tools for the thinking around your work.</p></div><Bot/></header><div className="ai-layout"><aside><h2>Tools</h2>{tools.map(x=><button key={x} aria-pressed={tool===x} onClick={()=>{setTool(x);setOutput('')}}>{x==='Contract'?<FileText/>:<Sparkles/>}{x}</button>)}<p>Deterministic local preview. No external AI service is called.</p></aside><section className="ai-work"><p className="eyebrow">{tool.toUpperCase()}</p><h2>Shape a useful first draft.</h2><label>What are you working on?<textarea value={subject} onChange={e=>setSubject(e.target.value)} minLength={4}/></label><button className="button primary" onClick={()=>{const value=generate(tool,subject);setOutput(value);setHistory(old=>[`${tool}: ${subject}`,...old].slice(0,5))}}><Sparkles/>Generate preview draft</button>{output&&<article className="ai-output"><span>Preview draft</span><pre>{output}</pre>{tool==='Contract'&&<p className="ai-legal">Template only—not legal advice. Ask an appropriately qualified professional to review it.</p>}<div><button className="button secondary" onClick={()=>{navigator.clipboard?.writeText(output);notify('Preview copied when browser permission allows.')}}><Copy/>Copy</button><button className="button secondary" onClick={()=>{toggleSaved({id:`${tool}-${subject}`,kind:'template',title:`${tool}: ${subject}`});notify('Draft saved to your local collection.')}}><Save/>Save</button><button className="button secondary" onClick={()=>navigate('tasks')}><Check/>Create task</button></div></article>}</section><aside className="ai-history"><h2>Recent</h2>{history.length?history.map(x=><button key={x} onClick={()=>setSubject(x.split(': ').slice(1).join(': '))}>{x}</button>):<p>Your local draft history will appear here.</p>}</aside></div></main>}
