import { X } from 'lucide-react'; import type { Creator } from '../types';
export default function TalentCompare({people,onClose}:{people:Creator[];onClose:()=>void}){return <aside className="talent-compare"><button className="icon-button" onClick={onClose} aria-label="Close comparison"><X/></button><p className="eyebrow">SIDE BY SIDE</p><h2>{people.length} creative{people.length===1?'':'s'} selected</h2><div>{people.map(person=><article key={person.id}><img src={person.image} alt=""/><strong>{person.name}</strong><span>{person.role}</span><small>{person.skills.slice(0,3).join(' · ')}</small><b>{person.availability}</b></article>)}</div></aside>}
