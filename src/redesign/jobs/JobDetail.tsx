import { ArrowLeft, Bookmark, CalendarDays, Clock, MapPin } from 'lucide-react';
import type { Job } from './data';
export default function JobDetail({job,saved,onBack,onSave,onApply}:{job:Job;saved:boolean;onBack:()=>void;onSave:()=>void;onApply:()=>void}){
 return <section className="job-detail"><button className="jobs-back" onClick={onBack}><ArrowLeft/> All opportunities</button><div className="job-detail-grid"><div><p className="eyebrow">{job.category} · {job.type}</p><h2>{job.title}</h2><p className="job-studio">{job.studio}</p><p className="job-summary">{job.summary}</p><div className="job-facts"><span><MapPin/>{job.location}</span><span><Clock/>{job.duration}</span><span><CalendarDays/>Apply by {job.deadline}</span></div><h3>What they need</h3><ul>{job.requirements.map(item=><li key={item}>{item}</li>)}</ul></div><aside><span>Project budget</span><strong>{job.budget}</strong><button className="button primary" onClick={onApply}>Start application</button><button className="button secondary" onClick={onSave}><Bookmark fill={saved?'currentColor':'none'}/>{saved?'Saved':'Save job'}</button><small>Application and saved states stay on this device.</small></aside></div></section>
}
